spring.application.name=Backend

# ============= JPA =============
spring.jpa.database=postgresql
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ============= EMAIL (Opcional) =============
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=tuCorreo@gmail.com
spring.mail.password=tuContrasenaDeAplicacion
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ============= DATABASE (Supabase - Session Pooler) =============
spring.datasource.driver-class-name=org.postgresql.Driver

# Render env var → DEBE venir completa con user & password dentro
spring.datasource.url=${DB_URL}

# Solo si NO usas password en la URL (en tu caso SÍ lo usas, pero no molesta)
spring.datasource.password=${DB_PASSWORD}

# Hikari
spring.datasource.hikari.maximum-pool-size=5

# Usuario de Supabase
spring.datasource.username=postgres.caztapsjivfkibobqjob

# ============= SERVER =============
server.port=${PORT:8080}

# ============= JWT =============
jwt.secret=${JWT_SECRET}
jwt.expiration=3600000
